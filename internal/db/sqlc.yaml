version: "2"
sql:
  - engine: "sqlite"
    queries: "queries.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "dbgen"
        out: "gen"
        emit_json_tags: true
        emit_empty_slices: true
        emit_interface: false
